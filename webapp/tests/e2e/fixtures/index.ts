/**
 * Shared fixtures and helpers for E2E tests.
 *
 * Extend the base test object here to add custom fixtures
 * (authenticated sessions, seeded data, etc.) that multiple
 * test files can reuse.
 */
export {};
